namespace BusinessObjectsLibrary.EFCore.NetCore {
    public static class MessageHelper {
        public const string OpenDatabaseFailed = "Make sure the database has been created.\r\nTo create the database, run the 'DatabaseUpdater' project.";
    }
}
